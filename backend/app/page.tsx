export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>PetTrack API</h1>
      <p>REST uçları <code>/api/*</code> altında. Arayüz için <code>frontend</code> paketini çalıştırın (varsayılan port 1570).</p>
    </main>
  );
}
