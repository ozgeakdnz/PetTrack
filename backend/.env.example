# Yerel Homebrew PostgreSQL (varsayılan kullanıcı: macOS kullanıcı adınız)
DATABASE_URL="postgresql://ozge@localhost:5432/pettrack"

# Docker Compose ile (docker compose up -d): port 5433
# DATABASE_URL="postgresql://pettrack:pettrack@localhost:5433/pettrack"

FRONTEND_ORIGIN=http://localhost:1575
